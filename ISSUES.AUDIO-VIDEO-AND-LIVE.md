# 音视频与直播问题总结

本文档汇总近期在桌面端 WebRTC 通话、摄像头采集、本地预览、直播房间接入过程中出现的问题、定位结论、解决方式以及当前状态。

## 1. 摄像头本地可预览，但无法发送给对方

### 现象

- 本地能看到摄像头预览。
- 对方看不到摄像头画面。
- 日志中出现类似信息：

```text
已收到首个本地原始视频帧，像素格式=Bgr, 尺寸=1280x720
本地视频错误: 当前 FFmpeg 编码后端不支持原始视频帧直送编码...
```

### 根因

- OpenCV 摄像头路径可以稳定拿到原始帧并驱动本地预览。
- 但自定义源通过 `FFmpegVideoSource.ExternalVideoSourceRawSample(...)` 直送原始帧进入编码器时，底层 FFmpeg 封装并不能稳定支持这条路径。
- 结果是“预览有画面，但发送链路停止”。

### 解决方式

- 不再把 OpenCV 原始帧作为主要发送链路。
- 摄像头发送改回优先使用 `FFmpegCameraSource`。
- OpenCV 路径仅保留为必要时的辅助能力，不再承担核心摄像头发送职责。

### 当前状态

- 对方已经可以看到本地摄像头画面。
- 说明摄像头发送链路已恢复。

---

## 2. 摄像头初始接通时没有画面，热切换后才有画面

### 现象

- 直接发起或接听通话后，对方一开始看不到本地摄像头。
- 只要在通话中执行一次“热切换到摄像头”，对方就能立刻看到画面。

### 根因

- `FFmpegCameraSource` 在视频格式协商完成前已经启动。
- 等协商得到视频格式后，只调用了 `SetVideoSourceFormat(...)`，但没有重建摄像头发送源。
- 热切换之所以生效，不是因为切换动作本身特殊，而是它触发了：

```text
停止当前视频源 -> 按已协商格式重新创建 -> 重新启动 -> 请求关键帧
```

### 解决方式

- 在 `WebRtcMediaSession` 中增加“视频格式协商后自动重启本地摄像头源”逻辑。
- 当满足以下条件时自动执行一次受控重启：
  - 当前使用摄像头而不是屏幕共享。
  - 当前视频源是 `FFmpegCameraSource`。
  - 视频已协商完成。
  - 本地视频源已经处于启动状态。
- 自动重启后，再补发一次本地视频关键帧请求。

### 当前状态

- 已实现自动重启逻辑。
- 目标是消除“必须手动热切换一次才能让对方看到画面”的现象。
- 该问题需要继续结合最新运行日志确认是否完全消除。

---

## 3. 对方能看到摄像头，但本地没有预览

### 现象

- 摄像头发送恢复后，对方已经可以看到本地画面。
- 但本地小窗预览仍然为空。

### 根因

- 当前摄像头发送主链路使用的是 `FFmpegCameraSource`。
- `FFmpegCameraSource` 负责采集和编码发送，但不会稳定产出本地原始帧事件。
- 本地预览原先依赖原始帧回调，因此发送恢复后预览仍可能为空。

### 解决方式

- 在 `WebRtcMediaSession` 中增加“本地预览回退解码”机制。
- 当本地没有原始预览帧，但已经拿到本地编码视频帧时：
  - 把本地编码帧送入一个额外的本地预览解码器。
  - 将解码后的结果回填到 `LocalVideoFrame`。
- 同时补齐两种回调路径：
  - 普通解码回调
  - 快速解码回调

### 当前状态

- 本地预览已经恢复正常。
- 说明“本地编码帧 -> 回退解码 -> 本地预览 UI”链路已打通。

---

## 4. 默认摄像头容易落到虚拟摄像头，导致初始化行为异常

### 现象

- 初始默认选中的摄像头可能是 `NVIDIA Broadcast` 等虚拟摄像头。
- 真正手动切换到实体摄像头后，发送行为才正常。

### 根因

- 摄像头枚举后默认直接取第一项。
- 某些机器上的设备排序会把虚拟摄像头排到实体摄像头之前。

### 解决方式

- 在桌面端 ViewModel 中调整默认摄像头优先级。
- 默认优先实体摄像头，例如：
  - USB
  - UVC
  - WebCam
  - Integrated Camera
- 将以下类型默认后置：
  - NVIDIA Broadcast
  - OBS Virtual Camera
  - Snap Camera
  - DroidCam
  - 其他明显虚拟设备

### 当前状态

- 已实现默认优先实体摄像头的策略。
- 这能显著降低初始落到虚拟摄像头导致“看似有设备、但行为异常”的概率。

---

## 5. 直播房间创建接口返回 404

### 现象

- 点击创建直播房间时，桌面端提示：

```text
Response status code does not indicate success: 404 (Not Found)
```

### 根因

- 客户端默认基地址配置为：

```text
https://krkr.chat
```

- 本地仓库里已经存在直播接口：
  - `/api/live-rooms`
  - `/api/live-rooms/{roomId}`
  - `/api/live-rooms/{roomId}/join`
  - `/api/live-rooms/{roomId}/leave`
  - `/api/live-rooms/{roomId}/signals`
  - `/ws/live-rooms/{roomId}`
- 但线上当前部署版本还没有这些接口，或反向代理尚未指向新版本服务，因此线上返回 404。

### 解决方式

- 本地联调时：
  - 将桌面端 `ApiBaseUrl` 指向本地服务端，例如 `http://localhost:5000`。
- 线上联调时：
  - 重新发布包含直播房间控制器和直播信令端点的服务端版本。
  - 确认反向代理或 Tunnel 未遗漏 `/api/live-rooms` 和 `/ws/live-rooms/*`。

### 当前状态

- 根因已确认，不是客户端 UI 按钮故障。
- 这是部署版本落后或路由未发布的问题。

---

## 6. 直播功能只有房间 CRUD，没有真实 WebRTC 观看链路

### 现象

- 早期直播模块只能：
  - 创建房间
  - 查询房间
  - 加入房间
  - 离开房间
- 但主播推流和观众观看并未真正进入 WebRTC 媒体链路。

### 根因

- 原有通话模型默认是双向通话。
- 直播场景至少需要区分两类角色：
  - 主播：只推流
  - 观众：只观看
- 此外，直播也不能直接复用普通 1:1 呼叫信令通道。

### 解决方式

- 增加媒体会话模式：
  - `SendReceive`
  - `PublishOnly`
  - `ViewOnly`
- 新增直播房间专用信令能力：
  - REST 信令接口
  - WebSocket 房间信令
- 桌面端接入直播专用媒体会话和直播专用画面状态。

### 当前状态

- 直播信令基础设施已经落地。
- 桌面端直播房间已接入最小 WebRTC 会话模型。
- 当前版本仍以“单主播 + 单观众”的 MVP 为边界。

---

## 7. 大补丁容易失败，导致修改难以稳定落地

### 现象

- 在 `MainWindowViewModel.cs` 这类大文件上一次性应用大量补丁时，容易因为上下文漂移失败。

### 根因

- 相关文件在本次会话中多轮修改。
- 大块补丁对上下文依赖强，轻微漂移就会导致 patch 失败。

### 解决方式

- 拆成小块、可验证的逐步改动：
  1. 先改媒体模式。
  2. 再改会话状态。
  3. 再改 UI 绑定。
  4. 每一步都立即编译验证。

### 当前状态

- 当前这套音视频和直播改动，已经采用“分块补丁 + 逐步编译验证”的方式稳定落地。

---

## 当前总体状态

### 已解决或基本解决

- 摄像头发送链路恢复。
- 本地摄像头预览恢复。
- 默认摄像头选择更偏向实体摄像头。
- 直播房间接口 404 的根因已明确。
- 直播房间最小 WebRTC 模型和房间信令已接入代码。

### 仍需继续确认或继续推进

- “初始接通无需手动热切换即可稳定发送摄像头”是否已经被彻底消除，需要继续结合最新运行日志确认。
- 直播功能当前仍是 MVP：单主播 + 单观众。
- 若后续需要多观众同时观看，需要引入 SFU 或主播对多个观众扇出多个 PeerConnection。

---

## 建议的后续验证顺序

1. 先验证普通通话摄像头：
   - 接通后不做热切换，确认对方是否直接看到本地摄像头。
   - 确认本地预览是否稳定显示。

2. 再验证直播房间：
   - 确认客户端连接的是本地新服务，还是线上旧服务。
   - 若使用线上域名，先确认服务端版本已发布。

3. 若普通通话仍存在“首次接通无画面”：
   - 继续把本地视频源启动时机后移到远端 SDP answer 应用之后。
